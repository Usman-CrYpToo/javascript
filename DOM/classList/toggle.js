const container = document.querySelector(".container");

container.classList.toggle("add"); // if already added it will remove the class 
container.classList.toggle("add");// add the the class if not added;
